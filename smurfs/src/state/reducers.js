import * as types from "./actions";

const intialState = {
  apiData: [],
  newPost: {},
  
};

export function dataReducer(state = intialState, action) {
  switch (action.type) {
   
    default:
      return state;
  }
}
